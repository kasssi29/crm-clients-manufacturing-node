# ===== Application config =====
PORT=3000
NODE_ENV=development

# ===== Database connection =====
# Replace <username> and <password> with your MongoDB Atlas user credentials
# Replace <cluster-url> with your cluster's hostname
# Example: mongodb+srv://myuser:mypassword@cluster0.xxxxx.mongodb.net/crm-back?retryWrites=true&w=majority
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/crm-back?retryWrites=true&w=majority

# ===== JWT =====
# Use a long, random string for signing tokens.
# Example (in Node.js REPL): require('crypto').randomBytes(32).toString('hex')
JWT_SECRET=your-secret-key-here

# ===== Initial credentials for seeding (for local dev only) =====
# In production, NEVER store real passwords in a file. Use env vars on the server.
ADMIN_EMAIL=admin@example.com
SUPERVISOR_EMAIL=supervisor@example.com
MANAGER1_EMAIL=manager1@example.com
MANAGER2_EMAIL=manager2@example.com
MANAGER3_EMAIL=manager3@example.com

ADMIN_PASSWORD=change-me
SUPERVISOR_PASSWORD=change-me
MANAGER1_PASSWORD=change-me
MANAGER2_PASSWORD=change-me
MANAGER3_PASSWORD=change-me
